# LatticeAI Goldset Answer Synthesis

- Snapshot: `data/kg_runs/latticeai_23`
- Model: `opus`
- Scenario count: 6

## Product-Validation Interpretation

This run supports the speed/cost thesis: KG-first retrieval gives Claude a compact evidence packet instead of asking it to rediscover the answer through open-ended repo search. That should be materially faster and cheaper than running Claude Code/Codex agentic search from scratch for each cross-repo question.

This run does not by itself complete product validation. The important question is answer quality. The scores below are generated by the synthesis layer and are useful as a first signal, but they must be checked against independent ground truth. When a generated answer misses ground truth, classify the failure as `missing KG fact`, `bad retrieval plan`, or `bad synthesis` before deciding the next implementation slice.

## Summary

| Scenario | Score | Failure Modes | Notes |
|---|---|---|---|
| Q082 | Pass | none | Answer enumerates client env/baseURL references across repos and ties the Apache vhost mapping to the mercury_api backend with the WSGI entrypoint, matching the expected shape with line-level citations. |
| Q083 | Pass | none | Answer enumerates backend token/auth routes and the corresponding mobile and web callers with file/line citations, matching the expected shape. |
| Q088 | Pass | none | Answer provides queue names, config references with line citations, the consumer handler for the message delivery queue, and the Zappa event source citation — matching the expected shape. The missing consumer for the scheduling queue is explicitly disclosed under unknowns rather than guessed. |
| Q095 | Pass | none | Answer covers the expected shape: domain→WSGI mapping (apache vhost), client baseURLs pointing to api.shopagain.io across mobile, UI, embed scripts, and internal services, plus backend repo (mercury_api) evidence — all with file/line citations. |
| Q100 | Pass | none | Answer enumerates documented endpoints lacking backend implementation and lacking client callers, with explicit drift caveats and citations to repo/path/line, matching the expected inventory + drift shape. |
| Q106 | Pass | none | Answer covers producer config references, Zappa consumer handler, queue name, and explicit unknowns matching the expected shape, all cited from packet evidence. |

## Q082 - Pass

**Question:** Which clients call api.shopagain.io, and which deployed backend serves that domain?

### Answer

## Clients calling `api.shopagain.io`

**Front-end / mobile clients**
- `ShopAgainMobile` — `VITE_API_ROOT=https://api.shopagain.io` in both `.env.development` and `.env.production` [ShopAgainMobile/.env.development:L1-1] [ShopAgainMobile/.env.production:L1-1].
- `mercury_ui` — `REACT_APP_API_ROOT=https://api.shopagain.io/` [mercury_ui/.env.production:L1-1]; also embedded in storefront scripts: `public/shopagain_script.min.js` [mercury_ui/public/shopagain_script.min.js:L1-1], `public/shopify/shopagain_script.js` [mercury_ui/public/shopify/shopagain_script.js:L32-32], `public/shopify/shopagain_script.min.js` [mercury_ui/public/shopify/shopagain_script.min.js:L1-1], and `public/shopify/widgets.js` [mercury_ui/public/shopify/widgets.js:L31-31].

**Backend services with outbound/self-references**
- `mercury_campaign_messages` — `api_url=https://api.shopagain.io` [mercury_campaign_messages/mercury_campaign_messages/configmanager/prod.ini:L8-8].
- `mercury_tracking` — `api_url=https://api.shopagain.io` [mercury_tracking/common/configmanager/prod.ini:L8-8].
- `mercury_webhooks` — `api_url=https://api.shopagain.io` [mercury_webhooks/common/configmanager/prod.ini:L28-28].
- `mercury_api` itself sets self-referential prod config: `API_URL` and `SMS_WEBHOOK_URL=https://api.shopagain.io/` [mercury_api/mercury_api/settings/shopagain_prod.py:L2-2] [mercury_api/mercury_api/settings/shopagain_prod.py:L4-4], plus `CSRF_COOKIE_DOMAIN`/`SESSION_COOKIE_DOMAIN=api.shopagain.io` [mercury_api/mercury_api/settings/shopagain_prod.py:L48-49].

**Docs**
- `shopagain_api_docs` lists it as the `acme-api` server in `dist.json` and `openapi/openapi.yaml` [shopagain_api_docs/dist.json:L14-14] [shopagain_api_docs/dist.json:L60-60] [shopagain_api_docs/docs/dist.json:L18-18] [shopagain_api_docs/openapi/openapi.yaml:L45-45].

## Backend serving `api.shopagain.io`

Apache vhost in `ansible-playbooks` routes the domain to a WSGI app in the `mercury_api` repo:
- Domain → deploy target: `wsgi:/home/ubuntu/mercury_api/mercury_api/prod_shopagain_wsgi.py` [ansible-playbooks/apache/prod_shopagain.conf:L2-7].
- Vhost references `api.shopagain.io` [ansible-playbooks/apache/prod_shopagain.conf:L7-7].

So the production backend is the **`mercury_api`** Django app, served via WSGI entrypoint `mercury_api/prod_shopagain_wsgi.py` on the `ubuntu` host.

### Caveats

- Some `mercury_ui` script files (`*.min.js`) only contain the bare hostname literal, not a full URL.
- `mercury_api`'s own references are configuration (cookie domains, self URL) rather than client calls.

### Unknown Because Missing Evidence

- Specific endpoints/paths each client hits are not in the packet.
- Host/IP and Apache server/listen directives beyond the WSGI alias are not captured.

### Score Notes

Answer enumerates client env/baseURL references across repos and ties the Apache vhost mapping to the mercury_api backend with the WSGI entrypoint, matching the expected shape with line-level citations.

## Q083 - Pass

**Question:** If token auth endpoints change in the backend, which web and mobile callers are affected?

### Answer

## Backend token/auth routes (mercury-api)

- `ANY /api/token/` [mercury_api/companies/urls.py:L63-63]
- `ANY /api/token/refresh/` [mercury_api/companies/urls.py:L64-64]
- `ANY /auth/` [mercury_api/companies/urls.py:L60-60]
- `ANY /auth/registration/` [mercury_api/companies/urls.py:L61-61], [mercury_api/companies/urls.py:L62-62]
- `ANY /auth/login/` [mercury_api/backoffice/urls.py:L17-17]
- `ANY /auth/` [mercury_api/woocommerce_app/urls.py:L6-6]
- `ANY /auth_internal/` [mercury_api/woocommerce_app/urls.py:L7-7]

## Mobile callers affected by token endpoint changes (ShopAgainMobile)

- `POST /api/token/` [ShopAgainMobile/src/api/login.api.tsx:L6-6]
- `POST /api/token/refresh/` [ShopAgainMobile/src/api/axiosConfig.tsx:L37-37]

Both mobile call sites match the backend `/api/token/` and `/api/token/refresh/` routes — any change to JWT token issue/refresh paths or payload contracts will break login and silent refresh on mobile.

## Web callers affected by broader auth endpoint changes (mercury-ui)

No direct caller of `/api/token/` or `/api/token/refresh/` is recorded for mercury-ui in the packet, but these auth-family callers depend on `/auth/*` routes:

- `POST /auth/logout/` [mercury_ui/src/services/auth.js:L14-14]
- `PATCH /auth/user/` [mercury_ui/src/services/auth.js:L23-23]
- `POST /auth/registration/` [mercury_ui/src/services/auth.js:L27-27]
- `POST /auth/registration/resend-email/` [mercury_ui/src/services/auth.js:L34-34]
- `POST /auth/account-confirm-email/` [mercury_ui/src/services/auth.js:L39-39]
- `POST /auth/password/reset/confirm/` [mercury_ui/src/services/auth.js:L45-45]
- `POST /auth/password/reset/` [mercury_ui/src/services/auth.js:L50-50]
- `POST /auth/password/change/` [mercury_ui/src/services/auth.js:L56-56]
- `GET /get_ggl_auth_login_status/` [mercury_ui/src/services/integration.js:L37-37]
- `POST /add_or_update_ggl_auth_integration/` [mercury_ui/src/services/integration.js:L40-40]
- `POST /remove_ggl_auth_integration/` [mercury_ui/src/services/integration.js:L43-43]
- `GET /get_judge_me_auth_login_status/` [mercury_ui/src/services/integration.js:L68-68]
- `POST /remove_judge_me_auth_integration/` [mercury_ui/src/services/integration.js:L71-71]

## Bottom line

- Changing `/api/token/` or `/api/token/refresh/` directly impacts **ShopAgainMobile** only (`login.api.tsx`, `axiosConfig.tsx`).
- Changing the wider `/auth/*` family impacts **mercury-ui** (`src/services/auth.js`, `src/services/integration.js`).

### Caveats

- Packet shows no mercury-ui caller for `/api/token/` or `/api/token/refresh/`; if web login uses session cookies via `/auth/login/` instead of JWT, that explains the asymmetry, but the packet does not confirm that.
- Routes in mercury-api are recorded as `ANY` method; method-level breakage cannot be determined from the packet.

### Unknown Because Missing Evidence

- Whether mercury-ui has any JWT token caller not captured in this packet.
- Exact handler/view behavior and request/response schemas for the token endpoints.

### Score Notes

Answer enumerates backend token/auth routes and the corresponding mobile and web callers with file/line citations, matching the expected shape.

## Q088 - Pass

**Question:** Which SQS queues connect campaign scheduling to message delivery, and who consumes each queue?

### Answer

## Campaign Pipeline SQS Queues

Two SQS queues bridge campaign scheduling and per-recipient message delivery, both referenced by `mercury-api` across its prod settings variants:

### 1. `sqs:la-prod-campaign` (campaign scheduling)
Referenced as `CAMPAIGN_SQS` by `mercury-api` in all prod profiles:
- [mercury_api/mercury_api/settings/azure_prod.py:L31-31]
- [mercury_api/mercury_api/settings/prod.py:L31-31]
- [mercury_api/mercury_api/settings/shopagain_prod.py:L31-31]

**Consumer:** No consumer fact for `la-prod-campaign` is present in the packet (see unknowns).

### 2. `sqs:la-prod-campaign-messages` (message delivery)
Referenced as `CAMPAIGN_MESSAGE_SQS` by `mercury-api`:
- [mercury_api/mercury_api/settings/azure_prod.py:L44-44]
- [mercury_api/mercury_api/settings/prod.py:L44-44]
- [mercury_api/mercury_api/settings/shopagain_prod.py:L44-44]

**Consumer:** `mercury-campaign-messages` consumes this queue via a Zappa SQS event source on the `prod` stage, dispatching to the handler `mercury_campaign_messages.email_sender.process_campaign_message_delivery` [mercury_campaign_messages/zappa_settings.json:L73-73].

### Flow
`mercury-api` → enqueue to `la-prod-campaign` (scheduling) → fan-out to `la-prod-campaign-messages` → consumed by `mercury-campaign-messages.email_sender.process_campaign_message_delivery`.

### Caveats

- All three mercury-api settings files (azure_prod, prod, shopagain_prod) declare identical queue names; the active file depends on deployment profile.
- The packet only confirms `mercury-api` references `la-prod-campaign`; the producer→consumer linkage on that first hop is inferred from naming/source_kind, not from a CONSUMES_EVENT fact.

### Unknown Because Missing Evidence

- Which service consumes `sqs:la-prod-campaign` (campaign scheduling queue) — no CONSUMES_EVENT fact in the packet.
- Whether `mercury-api` is the producer or only a config reference holder for these queues (no PRODUCES_EVENT fact provided).

### Score Notes

Answer provides queue names, config references with line citations, the consumer handler for the message delivery queue, and the Zappa event source citation — matching the expected shape. The missing consumer for the scheduling queue is explicitly disclosed under unknowns rather than guessed.

## Q095 - Pass

**Question:** If prod_shopagain_wsgi.py deployment changes, which public domains and clients are impacted?

### Answer

## Impact Summary

**Public domain affected:** `api.shopagain.io` routes to the WSGI target `wsgi:/home/ubuntu/mercury_api/mercury_api/prod_shopagain_wsgi.py` via the Apache vhost [ansible-playbooks/apache/prod_shopagain.conf:L2-L7]. Any change to this WSGI deployment directly affects every client/config that points at `api.shopagain.io`.

## Backend (serving repo)
- **mercury_api** — production settings reference the domain in multiple keys:
  - `API_URL = https://api.shopagain.io/` [mercury_api/mercury_api/settings/shopagain_prod.py:L2]
  - `SMS_WEBHOOK_URL = https://api.shopagain.io/` [mercury_api/mercury_api/settings/shopagain_prod.py:L4]
  - `CSRF_COOKIE_DOMAIN = api.shopagain.io` [mercury_api/mercury_api/settings/shopagain_prod.py:L48]
  - `SESSION_COOKIE_DOMAIN = api.shopagain.io` [mercury_api/mercury_api/settings/shopagain_prod.py:L49]

## Impacted Clients / Consumers

**Mobile app (ShopAgainMobile)**
- `VITE_API_ROOT = https://api.shopagain.io` [ShopAgainMobile/.env.development:L1]
- `VITE_API_ROOT = https://api.shopagain.io` [ShopAgainMobile/.env.production:L1]

**Web UI (mercury_ui)**
- `REACT_APP_API_ROOT = https://api.shopagain.io/` [mercury_ui/.env.production:L1]
- Embedded Shopify scripts/widgets call `api.shopagain.io`:
  - [mercury_ui/public/shopagain_script.min.js:L1]
  - [mercury_ui/public/shopify/shopagain_script.js:L32]
  - [mercury_ui/public/shopify/shopagain_script.min.js:L1]
  - [mercury_ui/public/shopify/widgets.js:L31]

**Other backend services (server-to-server callers)**
- `mercury_campaign_messages`: `api_url = https://api.shopagain.io` [mercury_campaign_messages/mercury_campaign_messages/configmanager/prod.ini:L8]
- `mercury_tracking`: `api_url = https://api.shopagain.io` [mercury_tracking/common/configmanager/prod.ini:L8]
- `mercury_webhooks`: `api_url = https://api.shopagain.io` [mercury_webhooks/common/configmanager/prod.ini:L28]

**API documentation (publicly published surface)**
- `shopagain_api_docs`: [shopagain_api_docs/dist.json:L14], [shopagain_api_docs/dist.json:L60], [shopagain_api_docs/docs/dist.json:L18], [shopagain_api_docs/openapi/openapi.yaml:L45]

## Decision-relevant takeaways
- A deploy change to `prod_shopagain_wsgi.py` will affect at minimum: the mobile app (dev+prod), the web UI (prod + Shopify embed scripts), and three internal services (campaign_messages, tracking, webhooks) — plus the published API docs surface.
- Cookie/session continuity is bound to `api.shopagain.io` (`CSRF_COOKIE_DOMAIN`, `SESSION_COOKIE_DOMAIN`); WSGI/app-config changes that affect cookie behavior will impact all browser clients on that host.

### Caveats

- Evidence is static-config only; runtime traffic patterns and any non-config callers (e.g., third-party integrations not represented in env/ini files) are not covered.
- Only the prod vhost mapping is shown; staging/dev WSGI targets, if any, are not in the packet.

### Unknown Because Missing Evidence

- Whether additional clients outside the indexed repos call api.shopagain.io.
- Whether the Shopify embed scripts in mercury_ui are served from the same WSGI or a CDN front.

### Score Notes

Answer covers the expected shape: domain→WSGI mapping (apache vhost), client baseURLs pointing to api.shopagain.io across mobile, UI, embed scripts, and internal services, plus backend repo (mercury_api) evidence — all with file/line citations.

## Q100 - Pass

**Question:** Which documented ShopAgain API endpoints are not obviously implemented or called by any client?

### Answer

## Documented endpoints with implementation or client gaps

### Not implemented by any scoped backend
- **`ANY /v1/store_data`** — documented in OpenAPI but appears as `left_only` against `mercury_api`/`mercury_webhooks` (no backend route) [shopagain_api_docs/openapi/openapi.yaml:88-88].
- **`ANY /v1/collections`** — no exact backend match; only a fuzzy `possible_match` to backend `/v1/product_collections` (similarity 0.789), so not obviously implemented [shopagain_api_docs/openapi/openapi.yaml:76-76] vs [mercury_api/mercury_api/urls.py:53-53].

### Not called by any scoped client
In the `clients_vs_docs_v1_endpoints` reconciliation, every documented `/v1/*` endpoint lands in `right_only` (i.e., no matched client call from `mercury_ui`, `ShopAgainMobile`, or `shopagain-chat-widget`):
- `ANY /v1/company` [shopagain_api_docs/openapi/openapi.yaml:67-67]
- `ANY /v1/contacts` [shopagain_api_docs/openapi/openapi.yaml:70-70]
- `ANY /v1/products` [shopagain_api_docs/openapi/openapi.yaml:73-73]
- `ANY /v1/collections` [shopagain_api_docs/openapi/openapi.yaml:76-76]
- `ANY /v1/carts` [shopagain_api_docs/openapi/openapi.yaml:79-79]
- `ANY /v1/checkouts` [shopagain_api_docs/openapi/openapi.yaml:82-82]
- `ANY /v1/orders` [shopagain_api_docs/openapi/openapi.yaml:85-85]
- `ANY /v1/store_data` [shopagain_api_docs/openapi/openapi.yaml:88-88]

### Net: documented but neither implemented nor called
- **`ANY /v1/store_data`** — fails both checks (no backend route, no client caller). Strongest drift candidate.
- **`ANY /v1/collections`** — implementation only matches via fuzzy rename to `/v1/product_collections`; no client caller observed.

### Caveats

- `right_only` in clients_vs_docs only means no client CALLS_ENDPOINT fact reconciled to the documented path; clients may use different path spellings or the evidence packet may not surface the underlying client facts.
- `/v1/collections` vs backend `/v1/product_collections` is a string-similarity guess (0.789); it could be the same endpoint renamed or two different ones.
- Backend scope is limited to `mercury_api` and `mercury_webhooks`; client scope is limited to `mercury_ui`, `ShopAgainMobile`, `shopagain-chat-widget`.

### Unknown Because Missing Evidence

- Whether any of the right_only documented endpoints are actually called by clients under non-`/v1/` prefixes or alternate spellings — the packet contains no CALLS_ENDPOINT evidence rows.
- Whether `/v1/collections` and `/v1/product_collections` are intended to be the same resource.

### Score Notes

Answer enumerates documented endpoints lacking backend implementation and lacking client callers, with explicit drift caveats and citations to repo/path/line, matching the expected inventory + drift shape.

## Q106 - Pass

**Question:** For la-prod-campaign-messages, who produces messages, who consumes them, and what evidence proves the edge?

### Answer

## Producer
`mercury-api` references the SQS channel `sqs:la-prod-campaign-messages` across all production settings modules (source_kind `CAMPAIGN_MESSAGE_SQS`):
- [mercury_api/mercury_api/settings/azure_prod.py:L44]
- [mercury_api/mercury_api/settings/prod.py:L44]
- [mercury_api/mercury_api/settings/shopagain_prod.py:L44]

These are configuration references identifying the queue mercury-api is wired to send to; the actual send-site code is not in the packet.

## Consumer
`mercury-campaign-messages` consumes `sqs:la-prod-campaign-messages` via a Zappa event source binding on the `prod` stage, dispatched to handler `mercury_campaign_messages.email_sender.process_campaign_message_delivery` [mercury_campaign_messages/zappa_settings.json:L73-L73]. The same file also carries the SQS ARN reference for the queue [mercury_campaign_messages/zappa_settings.json:L73-L73].

## Edge evidence
- Producer→queue: `REFERENCES_EVENT_CHANNEL` facts from mercury_api prod settings (3 paths above), source_kind=`CAMPAIGN_MESSAGE_SQS`.
- Queue→consumer: `CONSUMES_EVENT` fact from mercury_campaign_messages `zappa_settings.json` with `source_kind=zappa_event_source`, stage `prod`, function `process_campaign_message_delivery` [mercury_campaign_messages/zappa_settings.json:L73-L73].

## Queue identifier
Channel ID: `sqs:la-prod-campaign-messages` (queue name `la-prod-campaign-messages`). Full ARN string is not included in the packet — only that an `sqs_arn` reference exists at [mercury_campaign_messages/zappa_settings.json:L73-L73].

### Caveats

- mercury-api evidence is configuration-level (settings constant CAMPAIGN_MESSAGE_SQS); the packet does not include the actual SendMessage call site.
- Consumer binding is inferred from Zappa event_source config; runtime behavior not verified in the packet.

### Unknown Because Missing Evidence

- Exact SQS ARN value (account, region) for la-prod-campaign-messages.
- Producer code path / send-site within mercury-api that enqueues messages.
- Whether other repos also produce to this queue.

### Score Notes

Answer covers producer config references, Zappa consumer handler, queue name, and explicit unknowns matching the expected shape, all cited from packet evidence.
